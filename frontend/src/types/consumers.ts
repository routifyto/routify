export type ConsumerInput = {
  name: string;
  alias?: string | null;
  description?: string | null;
};

export type ConsumerOutput = {
  id: string;
  name: string;
  alias?: string | null;
  description?: string | null;
};
